Aufruf von UICreator mit Datenobjekt

liste erzeugen

Loop durch alle fields
	name auslesen
	type auslesen
	value auslesen
	Je nach Typ ein UIElement erzeugen
	value eintragen
	Je nach Typ eine Auslese-Funktion auswählen
	
	in liste speichern: name, type, value, uielement, function

		
bei apply oder ok
datenobjekt kopieren
Loop durch Liste
	value aus uielement auslesen
	zu type casten
	variable name auf value setzen

	